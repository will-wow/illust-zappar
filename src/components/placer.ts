import { BaseComponent, component } from "aframe-typescript-class-components";

/** A circle showing where to place a model. */
@component("placer")
export class PlacerComponent extends BaseComponent {}
